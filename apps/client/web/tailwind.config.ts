import type { Config } from "tailwindcss";
import { baseConfig } from "@piar/tailwind-config";

const config: Config = {
  ...baseConfig,
  // Add project-specific customizations here if needed
};

export default config;
