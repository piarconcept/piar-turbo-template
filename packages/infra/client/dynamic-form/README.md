# @piar/infra-client-dynamic-form

Dynamic form + table renderers for client applications.

## Purpose

Render UI from `@piar/domain-fields` configs, using `@piar/ui-components`.

This package is intended for the Backoffice (admin) but can be reused elsewhere.

## Status

WIP.
