# @piar/infra-client-dynamic-table

Minimal typed HTTP client helpers for metadata-driven ("dynamic") table endpoints.

This package is UI-agnostic; it only helps building request URLs and typed clients.
